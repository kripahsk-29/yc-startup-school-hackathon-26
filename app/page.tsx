import HomeClient from "./home-client";

// The demo-facing landing page: the story of TARA, told as an archival
// instrument record. All styling derives from design-inspo/PALETTE.md.
export default function Home() {
  return <HomeClient />;
}
